public static class CardPackGenerator
{
    public static Deck BasicCardPack()
    {
        // unfinished (return 9 random cards and one powercard)
        Deck basicPack = new Deck();
        return basicPack;
    }

    public static Deck PowerCardPack()
    {
        // unfinished returns 3 random powercards
        Deck powerPack = new Deck();
        return powerPack;
    }
}